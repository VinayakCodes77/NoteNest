[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "asgiref>=3.11.1",
    "django>=6.0.3",
    "gunicorn>=25.3.0",
    "sqlparse>=0.5.5",
    "tzdata>=2026.1",
    "whitenoise>=6.12.0",
]
